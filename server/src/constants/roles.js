const ROLES = {
  ADMIN: 'ADMIN',
  OFFICER: 'OFFICER',
  FORENSIC: 'FORENSIC',
  PROSECUTOR: 'PROSECUTOR',
  JUDGE: 'JUDGE'
};

const ALL_ROLES = Object.values(ROLES);

module.exports = {
  ROLES,
  ALL_ROLES
};
